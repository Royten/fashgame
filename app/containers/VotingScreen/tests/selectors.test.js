// import { fromJS } from 'immutable';
// import { makeSelectVotingScreenDomain } from '../selectors';

// const selector = makeSelectVotingScreenDomain();

describe('makeSelectVotingScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
