// import { fromJS } from 'immutable';
// import { makeSelectChallengeScreenDomain } from '../selectors';

// const selector = makeSelectChallengeScreenDomain();

describe('makeSelectChallengeScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
