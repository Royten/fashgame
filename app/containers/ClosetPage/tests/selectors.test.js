// import { fromJS } from 'immutable';
// import { makeSelectClosetPageDomain } from '../selectors';

// const selector = makeSelectClosetPageDomain();

describe('makeSelectClosetPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
